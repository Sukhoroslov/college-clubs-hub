<% layout('layout') %>

<div class="card">
  <div class="card-body">
    <h2>Welcome</h2>
    <p>Basic backend is set up. Use the API endpoints:</p>
    <ul>
      <li>GET /api/clubs</li>
      <li>GET /api/events</li>
      <li>POST /api/clubs</li>
      <li>POST /api/events</li>
    </ul>
    <p>Run seed script: <code>npm run seed</code> (make sure MONGO_URI is set)</p>
  </div>
</div>